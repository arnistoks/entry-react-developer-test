import React from 'react';
import './Cart.scss';

const Cart = () => {
    const a = 123;

    return (
        <section className="cartSection">
            Cart
        </section>
    );
};

export default Cart;